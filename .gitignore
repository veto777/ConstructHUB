node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Photo Optimizer runtime data — recreated at runtime, never deploy
tmp/
uploads/

# Local env / cache
.env
.env.local
.cache/
.turbo/
