node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Photo Optimizer runtime data — recreated at runtime, never deploy
tmp/
uploads/

# Local env / cache
.env
.env.local
.cache/
.turbo/

# NETR scraper checkpoint (scratch)
server/data/.scrape-progress.json
scripts/_smoke-seed.ts
