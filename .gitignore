node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Photo Optimizer runtime data — recreated at runtime, never deploy
tmp/
uploads/

# Local env / cache
.env
.env.local
.cache/
.turbo/

# NETR scraper checkpoint (scratch)
server/data/.scrape-progress.json
scripts/_smoke-seed.ts

# HCP competitive-analysis crawl — contains live account data/PII, NEVER commit
analysis/

# permit-portal workflow scratch
server/data/_permit-candidates.json
scripts/_*.ts
scripts/_verify-round2.ts

# live CF DNS token + DNS rollback snapshots — never commit
secrets/
tunnel/dns-rollback/
